# react-to-lit: Blockers & Improvements

**Date:** 2026-04-12
**Status:** 441/441 tests passing | Gate 2: 6/91 clean (1572 TS errors) | Gate 3: 91/91 passing

**Note:** Previous "89/91 clean" metric was inflated — syntax errors from invalid
`unknown<T>` types caused tsc to skip semantic analysis for the entire project.
After fixing the React type cleanup (rule 6 + catch-all), tsc now runs full
semantic checks, revealing the true error count.

**Progress:**
| Metric | Start of day | Current | Delta |
|--------|-------------|---------|-------|
| Clean components | 7 | 6 | -1 (honest baseline) |
| Total TS errors | 1753 | 1572 | -181 |
| Gate 3 passing | 90/91 | 91/91 | +1 |

---

## Blockers

### B1. Orphaned identifiers (1241x TS2304, 84 components)

"Cannot find name 'xxx'" — variables referenced in handlers, helpers, and effects
that have no declaration in scope. Sources:
- Body preamble variables filtered out by the React-pattern/safety filter
- Imported constants (e.g. `KeyCode`, `warnOnce`) not tracked
- Variables from `inline` action hooks

---

### B2. Slot props excluded from `this.` rewriting (129x TS2663, 48 components)

"Did you mean the instance member 'this.xxx'?" — slot props like `children`,
`header`, `description` aren't in the member map, so they don't get `this.`
prefix in handler/helper bodies.

**Complication:** Simply adding slots to the member map causes 205 new TS2339
errors because slots aren't declared as class fields. Fix requires declaring
slot props as class properties first, then adding to member map.

**Files:** `src/transforms/identifiers.ts:41-43`, `src/emitter/properties.ts`

---

### B3. `ref.current` pattern mismatch (48x TS2339 + 21x TS2345 + 2x TS2741, 17 components)

Non-DOM refs emitted as `private _fooRef = { current: value }` but `ref.current`
→ `this._fooRef` rewriting strips `.current`, producing type mismatches.

**Files:** `src/transforms/identifiers.ts:203-209`, `src/emitter/properties.ts:164-180`

---

### B4. Shorthand property references (57x TS18004, 16 components)

`{ funnelInteractionId }` references skipped hook vars that are now class fields
(`this._funnelInteractionId`), but `isDeclarationPosition` returns true for
shorthand properties, blocking the rewrite.

**Files:** `src/transforms/identifiers.ts:392`

---

### B5. State setter / ref type mismatches (32x TS2322, 23 components)

`setFoo(prev => !prev)` becomes `this._foo = prev => !prev` (assigns function).
Also ref assignments like `fooRef.current = 5` become `this._fooRef = 5` against
`{ current: number }` type.

**Files:** `src/transforms/identifiers.ts:186-199`

---

### B6. Minor type issues (44 errors across various categories)

- TS2315 (13x): Generic type misuse
- TS2554 (7x): Wrong argument count
- TS2729 (6x): Property used before initialization
- TS1345 (4x): Void tested for truthiness
- Others (14x): Assorted edge cases
